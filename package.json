{
  "name": "habit-tracker-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @habit-tracker/web",
    "dev:api": "npm run dev --workspace @habit-tracker/api",
    "build": "npm run build --workspace @habit-tracker/web && npm run build --workspace @habit-tracker/api"
  }
}
